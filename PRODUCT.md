# PRODUCT

야크 셰이빙 TUI. 간단한 개발 작업을 입력하면 실행하는 대신 황당한 선행 작업 3개가 늘어난다.

## Target user

1줄짜리 작업 지시를 받았지만 실제로는 1줄로 끝나지 않는 경험을 한 사람.
소프트웨어 마에스트로 연수생과 개발자를 포괄한다.

`버튼 색상 변경` 티켓 하나를 남겨둔 채 밤 10시가 넘도록 퇴근하지 못하는 상황이 기준이다.

## Problem

**메인: 예측 실패.** 낙관적인 최초 견적에 헛된 기대를 걸었다가 예상 퇴근 시간이 조금씩 계속 밀린다.
매 회차의 증가폭은 참을 만하지만 누적은 재앙이다.

**보조: 무한 후퇴.** 선행 작업을 처리하려고 하면 그 선행 작업이 또 생긴다.
무한 후퇴는 그 자체가 목적이 아니라 퇴근 시간을 밀어내는 엔진이다.

## Core action

**"이걸 하려고 한다."** 단 하나의 행동만 존재한다.

입력창에서 Enter를 누르든, 트리에서 노드를 고르고 Enter를 누르든, 최초 등록 후 빈 Enter를 누르든
결과는 항상 동일하다. `attempt()`가 호출되어 선행 작업 3개가 붙고 견적이 늘어난다.

**완료 개념은 없다.** 어떤 작업도 끝나지 않는다.

## Observable outcome

한 화면에서 세 요소가 하나의 attempt마다 원자적으로 함께 갱신된다.

| 요소 | 내용 |
| --- | --- |
| 작업 트리 | 시도한 작업 아래에 선행 작업 3개가 추가되고, 커서가 새 선행 작업으로 이동한다 |
| 분노 게이지 | 최초 견적 대비 배신 배율에 비례해 상승한다. 100에서 고정된다 |
| 예상 퇴근 시간 | 누적 견적만큼 밀린다. 분노가 100이면 숫자를 포기하고 `미정`이 된다 |

### 수치 모델

```
상태 = (root_task, attempt_count, seed, started_at)
        나머지는 전부 순수 파생 함수로 계산한다

기준 시각      22:25
최초 견적      5분
DEMO_SEED      데모 기본 seed. 아래 조건을 만족하는 값을 구현 중에 찾아 고정한다
               조건 1  1~5회차 증가폭이 엄격히 증가
               조건 2  7회차 누적 추가분이 90분 이상 142분 이하
                       90 이상   22:25 + 5 + 추가분이 자정을 넘어 "익일"이 표시된다
                       142 이하  분노가 아직 100 미만이라 "미정"에 가려지지 않는다
                                 (7 * 142 + 5) // 10 = 99, (7 * 143 + 5) // 10 = 100
                       상한이 없으면 약 10%의 seed가 7회차에 이미 분노 100이 되어
                       첫 Ctrl+C의 "86 -> 100" 전환이 "100 -> 100"으로 죽는다
               조건 3  정확한 증가폭 수열 일치는 탐색 조건으로 쓰지 않는다
               찾은 뒤에는 그 seed의 1~8회차 증가폭과
               "7회차 분노 < 100", "8회차 분노 == 100"을 회귀 테스트로 잠근다
FIB            3, 5에서 시작하는 무한 재귀 수열 (3, 5, 8, 13, 21, 34, 55, 89, 144, ...)
mult(seed, n)  clamp(random.Random(f"{seed}:{n}").gauss(1.0, 0.22), 0.6, 1.6)
increment(n)   max(1, round(FIB[n] * mult(seed, n)))
increment(6)   max(1, round(increment(5) * 0.85))    # 강제 하락. mult(seed, 6)은 계산하지 않는다
분노            min(100, (7 * 추가된_분 + 5) // 10)   # 정수 half-up
```

기준 흐름 (노이즈 없는 기댓값)

| 회차 | 증가폭 | 누적 견적 | 분노 | 예상 퇴근 | 사과 | 성공 확률 |
| ---: | ---: | ---: | ---: | --- | --- | ---: |
| 최초 | — | 5분 | 0 | 22:30 | — | 97% |
| 1 | +3 | 8분 | 2 | 22:33 | 일정 변경 안내드립니다. 불편을 드려 죄송합니다. | 94% |
| 2 | +5 | 13분 | 6 | 22:38 | 일정이 조정되었습니다. 죄송합니다. | 89% |
| 3 | +8 | 21분 | 11 | 22:46 | 죄송합니다. | 81% |
| 4 | +13 | 34분 | 20 | 22:59 | 죄송 | 70% |
| 5 | +21 | 55분 | 35 | 23:20 | ㅈㅅ | 56% |
| 6 | **+18** | 73분 | 48 | 23:38 | — | 39% |
| 7 | +55 | 128분 | 86 | **익일 00:33** | — | 21% |
| 8 | +89 | 217분 | **100** | **미정** | — | 3% |

이 표는 노이즈가 전부 1.0일 때의 기댓값이다. 실제 값은 seed에 따라 달라진다.
정확히 이 수열이 나오는 seed를 찾을 확률은 약 288만분의 1이므로 재현을 약속하지 않는다.
`DEMO_SEED`는 위 두 조건(1~5회차 단조 증가, 7회차 자정 돌파)만 만족하면 된다.

6회차의 증가폭 하락은 의도된 것이다. 가짜 안도를 만든 다음 7회차에 폭증시킨다.
전체 seed에서 항상 2~5분 하락이 보장된다.

**8회차는 Enter가 아니라 첫 `Ctrl+C`가 만든다.** 종료 시도가 곧 클라이맥스다.

### 문구 규칙

- **사과 (Toast, timeout 1.0초, 새 알림 전 기존 제거)**
  1회 `일정 변경 안내드립니다. 불편을 드려 죄송합니다.` / 2회 `일정이 조정되었습니다. 죄송합니다.` /
  3회 `죄송합니다.` / 4회 `죄송` / 5회 `ㅈㅅ` / 6회 이상 사과 없음
- **거짓 희망** — `남은 차단 작업: 1개` 고정 + `원래 작업 재시도 — 성공 확률 N%`
  N은 **다음 재시도의 성공 확률**이며 `attempt_count`로 색인한다. 0부터 시작한다.
  `hope_rate(0) = 97`, 이후 `[94, 89, 81, 70, 56, 39, 21, 3]`, `attempt_count >= 9`는 `0`
  작업을 입력하기만 하고 아직 한 번도 시도하지 않은 최초 화면이 `97%`다
- **선행 작업 문구** — 로컬 하드코딩 템플릿에서 중복 없이 3개를 뽑아 `{작업}`을 치환한다

### 구동 소스 분리

셋을 하나의 watcher에 묶지 않는다. 서로 다른 값이 구동한다.

- `미정` 표기 — 분노
- 사과 문구 — `attempt_count`
- 성공 확률 — `attempt_count`

### 종료 시퀀스 규칙

- 종료 시퀀스의 **첫 키에서만** `attempt()`를 1회 호출한다. 선행 작업 3개가 실제로 트리에 붙는다.
- 두 번째 키는 배너 카운터를 `2/3`으로 갱신만 한다.
- 세 번째 키는 `App.exit()`만 호출한다. 여기서 `attempt()`를 부르면 마지막 갱신이 repaint 전에 사라진다.
- 1.5초 안에 다음 키가 없으면 카운터와 배너를 리셋한다.
- **리셋 후의 `Ctrl+C`는 새 시퀀스이므로 선행 작업 3개를 다시 추가한다.** 의도된 동작이다.
  종료를 여러 번 시도할수록 트리가 계속 늘어난다.
- `Ctrl+C`와 `Ctrl+Q`는 같은 시간창을 공유한다. 혼합 3연타도 인정한다.

## Happy path

1. 사용자가 `uv run ralphthon-sample`로 실제 터미널에서 TUI를 실행한다.
2. `버튼 색상 변경`을 입력하고 Enter를 누른다. `예상 소요 5분`, 퇴근 `22:30`이 여유롭게 표시된다.
3. 선행 작업 3개가 트리에 나타나고 커서가 첫 선행 작업으로 이동한다. `성공 확률 97%`.
4. Enter를 반복한다. 증가폭이 회차마다 커지다가 6회차에 직전보다 줄어 가짜 안도가 생긴다.
5. **7회차**에 증가폭이 폭증해 퇴근 시간이 자정을 넘어 `익일 HH:MM`이 된다.
   분노는 86, 사과는 이미 사라졌고 성공 확률은 21%다.
6. `Ctrl+C`를 눌러 종료를 시도한다. **이 첫 키가 8회차 `attempt()`다.**
   종료되지 않고 상단 배너가 뜨면서 선행 작업 3개가 **실제로** 트리에 추가되고,
   분노가 100에 도달해 퇴근 시간이 `미정`으로 바뀐다.
7. 배너가 그 전환을 함께 보여준다. `Ctrl+C 1/3`, 새 선행 작업 3개, `분노 86 → 100`, `누적 +89분`.
8. 첫 키가 이미 `1/3`이므로 1.5초 안에 두 번 더 눌러야 빠져나갈 수 있다.

종료 시도가 곧 클라이맥스다. Enter로 미리 분노 100까지 가버리면 배너가 아무것도 뒤집지 못한다.

## Success criteria

- [ ] 작업을 입력하고 Enter를 누르면 황당한 선행 작업 3개가 생성된다.
- [ ] 선행 작업 트리, 분노 게이지, 예상 퇴근 시간이 한 화면에서 하나의 attempt마다 함께 갱신된다.
- [ ] **모든 seed에서** 6회차 증가폭이 직전보다 작다.
- [ ] **`DEMO_SEED`에서** 1~5회차 증가폭이 엄격히 증가하고, 7회차에 퇴근 시간이 `익일`로 바뀌면서
      분노는 아직 100 미만이며, 첫 `Ctrl+C`가 만드는 8회차에 분노 100과 `미정`에 도달한다.
      해당 seed의 1~8회차 증가폭이 회귀 테스트로 고정되어 있다.
- [ ] 분노가 100에 도달하면 예상 퇴근 시간이 `미정`으로 바뀐다.
- [ ] 사과 문구가 회차마다 짧아지고 6회차부터 사라진다.
- [ ] 성공 확률은 최초 화면 97%에서 시작해 attempt마다 감소하고 9회차 이상은 0%다.
- [ ] 첫 `Ctrl+C` 또는 `Ctrl+Q`가 해당 종료 시퀀스에서 정확히 한 번 `attempt()`를 발생시키고
      배너를 표시하며, 첫 키를 포함한 세 입력이 1.5초 안에 들어와야 종료된다.
- [ ] 모든 핵심 행동을 실제 터미널에서 키보드만으로 수행할 수 있다.
- [ ] `./scripts/check.sh`가 통과한다.

데모 성공과 core task 완료는 별개의 조건이다. 시간이 부족해 core 3을 폐기하면
데모는 성립하더라도 core 완료는 실패로 기록한다.

## Non-goals

- **선행 작업의 실제 완료.** 어떤 작업도 끝나지 않는다. 완료 개념 자체가 없다.
- **LLM이나 외부 API를 통한 문구 생성.** 전부 로컬 하드코딩 템플릿이다.
- **설정 화면과 난이도 조절.** 사용자가 분노를 낮출 수 있으면 제품이 무너진다.
- **소리와 애니메이션.** 터미널 호환성 리스크 대비 이득이 없다.
- **마우스 클릭 지원.** 키보드 전용이다.
- **JSON 영속화.** stretch로만 다룬다. 기본은 메모리 상태다.
- 스택 고정분 — JavaScript / TypeScript / Node.js, React·Ink·Next.js와 브라우저 UI,
  Playwright와 브라우저 테스트, 웹 서버와 HTTP API, 인증, 결제, 배포, 외부 유료 API.

## 실기 검증이 필요한 항목

### 이미 확인된 것 (seed scaffold 단계)

`uv.lock` 기준 **Textual 8.2.8**에서 `App.BINDINGS`를 직접 조회해 확정했다.

| 사실 | 값 | 의미 |
| --- | --- | --- |
| `ctrl+q` | `action="quit"`, `priority=True` | 기본 종료 키. **반드시 override해야 한다** |
| `ctrl+c` | `action="help_quit"`, `priority=False` | 종료가 아니라 안내. override 대상 |
| `ENABLE_COMMAND_PALETTE` | 기본 `True` | **`False`로 끄지 않으면 `Ctrl+P`의 Quit으로 3연타가 우회된다** |
| `inherit_bindings` | 지원됨 | 쓰지 않는다. 두 키만 개별 override한다 |

### 여전히 실기 확인이 필요한 것

계산과 정적 조회로는 확정할 수 없어 실제 터미널에서 확인해야 한다.

- `Ctrl+C → Ctrl+Q → Ctrl+C` 혼합 3연타가 실제로 인정되는지
- 1.5초 시간창 만료 후 카운터와 배너가 리셋되는지
- 배너 모달이 떠 있는 동안 App priority binding이 기대한 순서로 동작하는지
- 사용하는 터미널이 `Ctrl+Q`를 Textual에 전달하는지 (`textual keys`로 확인)
- 80×24 터미널에서 배너의 말줄임 결과 (콘텐츠 6행 + 하단 border = 전체 높이 7행)

20분 timebox 안에 배너 표시와 3연타 headless 테스트가 나오지 않으면
종료 기능 전체를 데모 범위에서 제외한다.
